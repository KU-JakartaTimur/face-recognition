# Dependencies untuk main.py (service FastAPI face recognition).
#
# CATATAN: paket `face_recognition` sengaja TIDAK dicantumkan di sini.
# Metadata-nya mewajibkan distribusi `dlib`, yang di-build dari source
# (butuh cmake + ~15 menit compile). Dockerfile di project ini memasang
# `dlib-bin` (wheel siap pakai, modul Python-nya tetap bernama `dlib`)
# lalu menginstal face_recognition dengan --no-deps.
#
# Instalasi manual di luar Docker:
#   pip install -r requirements.txt
#   pip install --no-deps face_recognition==1.3.0

fastapi==0.115.6
python-dotenv==1.0.1
uvicorn[standard]==0.32.1
gunicorn==22.0.0
numpy==1.26.4
Pillow==10.4.0
opencv-python-headless==4.10.0.84
dlib-bin>=19.24.2
face-recognition-models==0.3.0
# face_recognition_models mengimpor pkg_resources (bagian dari setuptools).
# Sejak Python 3.12 venv tidak lagi memasang setuptools otomatis, jadi harus
# disebut eksplisit. Pin <81 karena setuptools 81+ men-deprecate pkg_resources.
setuptools<81
